apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  labels:
    app: postgres
data:
  POSTGRES_DB: postgresdb
  POSTGRES_USER: postgresadmin
  POSTGRES_PASSWORD: admin123

  # psql -h localhost -U postgresadmin --password -p 5444 postgresdb
  # psql -h localhost -U postgresadmin -p 5444 postgres