apiVersion: v1
kind: Service
metadata:
  name: bookmark-service
  labels:
    app: bookmark-app
spec:
  type: ClusterIP
  selector:
    app: bookmark-app
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 5000